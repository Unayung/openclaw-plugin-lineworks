// LINE WORKS credentials are supplied through channel config / env vars only.
// No SecretRef wiring is exposed yet.
export const secretTargetRegistryEntries: readonly [] = [];

export function collectRuntimeConfigAssignments(): void {}
